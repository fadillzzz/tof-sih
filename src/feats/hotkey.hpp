namespace Feats {
    namespace Hotkey {
        void init();
        void tick();
        void menu();

        bool hotkeyPressed(const std::string &key);
    } // namespace Hotkey
} // namespace Feats
