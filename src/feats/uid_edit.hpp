namespace Feats {
    namespace UidEdit {
        const std::string confPrefix = "/feats/uidEdit";
        const std::string confToggleEnabled = confPrefix + "/toggleEnabled";

        void init();
        void tick();
        void menu();
    } // namespace UidEdit
} // namespace Feats