namespace Feats {
    namespace Ping {
        const std::string confEnabled = "/feats/ping/enabled";
        const std::string confToggleEnabled = "/feats/ping/toggleEnabled";

        void init();
        void tick();
        void menu();
    } // namespace Ping
} // namespace Feats
